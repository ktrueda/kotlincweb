apiVersion: v1
kind: Service
metadata:
  name: {{ include "kotlincweb.fullname" . }}
  labels:
    {{- include "kotlincweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kotlincweb.selectorLabels" . | nindent 4 }}
